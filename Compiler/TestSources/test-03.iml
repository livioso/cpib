program main
global
    position: record ( x:int64, y:int64 )
    professor: record ( id:int64, level:int64 )

do
    position.x init := 4;
    position.y init := 5;

    professor.id init := 1007;
    professor.level init := 19;

    debugin position.y;
    position.x := 42;

    offsetInY init := 5;
    position.y := position.y + offsetInY

endprogram